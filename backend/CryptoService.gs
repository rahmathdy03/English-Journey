function randomToken_(){return Utilities.getUuid()+Utilities.getUuid()+Utilities.getUuid()}function hashText_(value){const bytes=Utilities.computeDigest(Utilities.DigestAlgorithm.SHA_256,String(value),Utilities.Charset.UTF_8);return bytes.map(function(b){return('0'+((b<0?b+256:b).toString(16))).slice(-2)}).join('')}function hashPin_(userId,pin){const salt=PropertiesService.getScriptProperties().getProperty('PIN_SALT');if(!salt)throw appError_('PIN salt is not configured. Run setupEnglishJourney().','SETUP_REQUIRED');return hashText_(salt+'|'+userId+'|'+pin)}function safeEqual_(a,b){a=String(a||'');b=String(b||'');if(a.length!==b.length)return false;let x=0;for(let i=0;i<a.length;i++)x|=a.charCodeAt(i)^b.charCodeAt(i);return x===0}
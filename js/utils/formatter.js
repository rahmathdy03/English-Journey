export const formatPercent=v=>`${Math.round(Number(v||0))}%`;export const formatScore=v=>Number.isFinite(Number(v))?Math.round(Number(v)):0;export function formatDate(v,o={day:'numeric',month:'short',year:'numeric'}){if(!v)return'—';const d=new Date(v);return Number.isNaN(d.getTime())?'—':new Intl.DateTimeFormat('id-ID',o).format(d)}export function greetingByHour(h=new Date().getHours()){if(h<11)return'Good morning';if(h<15)return'Good afternoon';if(h<19)return'Good evening';return'Good night'}
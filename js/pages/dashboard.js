import{initProtectedPage,pageHeader}from'../app.js';import{userApi}from'../api/user-api.js';import{loader,errorState}from'../components/page-loader.js';import{statCard}from'../components/progress-card.js';import{refreshIcons}from'../utils/dom.js';import{escapeHTML}from'../utils/sanitizer.js';
const root=initProtectedPage({active:'dashboard',title:'Dashboard'});if(root)load();
async function load(){root.innerHTML=loader();try{const d=await userApi.getDashboard(),u=d.user,p=d.progress,t=d.today,partner=d.partner||{};root.innerHTML=`${pageHeader(`${d.greeting}, ${escapeHTML(u.displayName)}!`,'Ready for another small step today?','<a class="btn btn-primary" href="learning.html"><i data-lucide="play"></i> Continue learning</a>')}<section class="hero-dashboard card"><div><span class="badge">Week ${t.week} · Day ${t.day}</span><h2>${escapeHTML(t.title||'Today’s lesson')}</h2><p>${escapeHTML(t.subtitle||'Vocabulary, grammar, and practice are waiting for you.')}</p><div class="cluster"><a class="btn btn-primary" href="learning.html">Open learning path</a><a class="btn btn-ghost" href="progress.html">View progress</a></div></div><img src="../assets/images/illustrations/study-together.svg" alt="Two learners studying together"></section><section class="stats-grid">${statCard('flame','Current streak',p.currentStreak||0)}${statCard('trophy','Best streak',p.bestStreak||0,'yellow')}${statCard('languages','Words learned',p.totalWords||0,'pink')}${statCard('target','Average score',`${Math.round(p.averageScore||0)}%`,'lavender')}</section><section class="dashboard-grid"><article class="card"><div class="section-heading"><div><p class="eyebrow">Today</p><h2>Lesson checklist</h2></div><span class="badge ${t.dailyCompleted?'success':''}">${t.dailyCompleted?'Completed':'In progress'}</span></div>${check('Vocabulary',t.vocabularyCompleted)}${check('Grammar',t.grammarCompleted)}${check('Practice',t.practiceCompleted)}</article><article class="card partner-summary"><p class="eyebrow">Together</p><h2>${escapeHTML(partner.displayName||'Partner')}’s progress</h2><div class="partner-row"><img src="../assets/images/avatars/${partner.avatar||'rahmat.svg'}" alt=""><div><strong>${partner.currentStreak||0} day streak</strong><p class="muted">${partner.totalWords||0} words learned · ${Math.round(partner.averageScore||0)}% average</p></div></div><p class="couple-quote">${escapeHTML(d.coupleMessage||'Keep growing together.')}</p><a class="text-link" href="compare-progress.html">Compare progress positively →</a></article></section>`;refreshIcons()}catch(e){root.innerHTML=errorState(e.message);root.querySelector('[data-retry]')?.addEventListener('click',load)}}
function check(label,done){return`<div class="check-row"><span class="check-icon ${done?'done':''}"><i data-lucide="${done?'check':'circle'}"></i></span><span>${label}</span><strong>${done?'Done':'Not yet'}</strong></div>`}
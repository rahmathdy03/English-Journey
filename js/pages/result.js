import{initProtectedPage,pageHeader}from'../app.js';import{quizApi}from'../api/quiz-api.js';import{loader,errorState,emptyState}from'../components/page-loader.js';import{queryNumber}from'./page-utils.js';import{scoreLabel}from'../services/score-service.js';import{refreshIcons}from'../utils/dom.js';import{escapeHTML}from'../utils/sanitizer.js';
const root=initProtectedPage({active:'practice',title:'Result'}),week=queryNumber('week',1),day=queryNumber('day',1);if(root)load();async function load(){root.innerHTML=loader('Loading saved result…');try{const d=await quizApi.getResult(week,day);if(!d.quizzes?.length){root.innerHTML=pageHeader('Result',`Week ${week} · Day ${day}`)+emptyState('No result yet','Complete at least one quiz to see your result.');return}root.innerHTML=`${pageHeader('Learning Result',`Week ${week} · Day ${day}`)}<section class="result-hero card"><div class="score-ring"><strong>${Math.round(d.finalScore||0)}</strong><span>/100</span></div><div><span class="badge success">Progress saved</span><h2>${scoreLabel(d.finalScore||0)}</h2><p>Your scores were verified and stored by Google Apps Script.</p></div></section><section class="result-grid">${d.quizzes.map(q=>scoreCard(q)).join('')}</section><section class="card"><div class="section-heading"><div><p class="eyebrow">Review</p><h2>Questions to revisit</h2></div><a class="btn btn-secondary" href="review.html">Review mistakes</a></div>${d.mistakes?.length?d.mistakes.map((m,i)=>`<details class="mistake-item"><summary>${i+1}. ${escapeHTML(m.prompt)}</summary><p><b>Your answer:</b> ${escapeHTML(m.userAnswer||'No answer')}</p><p><b>Correct answer:</b> ${escapeHTML(m.correctAnswer)}</p><p>${escapeHTML(m.explanation||'Review this item and try again.')}</p></details>`).join(''):'<p class="success-text">Perfect—no mistakes in the saved quizzes.</p>'}</section><div class="result-actions"><a class="btn btn-ghost" href="dashboard.html">Back to dashboard</a><a class="btn btn-primary" href="learning.html">Continue learning</a></div>`;refreshIcons()}catch(e){root.innerHTML=errorState(e.message);root.querySelector('[data-retry]')?.addEventListener('click',load)}}function scoreCard(q){return`<article class="card score-card"><span>${q.quizType}</span><strong>${Math.round(q.score||0)}%</strong><small>${q.correctCount||0} correct · ${q.wrongCount||0} wrong</small></article>`}
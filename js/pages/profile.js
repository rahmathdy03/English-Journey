import{initProtectedPage,pageHeader}from'../app.js';import{userApi}from'../api/user-api.js';import{loader,errorState}from'../components/page-loader.js';import{showToast}from'../components/toast.js';import{logout}from'../auth/logout.js';import{updateSessionUser}from'../auth/session.js';import{refreshIcons}from'../utils/dom.js';import{escapeHTML}from'../utils/sanitizer.js';
const root=initProtectedPage({active:'profile',title:'Profile'});if(root)load();async function load(){root.innerHTML=loader('Loading profile…');try{const d=await userApi.getProfile();root.innerHTML=`${pageHeader('Profile','Update personal learning targets.')}<section class="profile-layout"><article class="card profile-card"><img src="../assets/images/avatars/${d.avatar}" alt=""><h2>${escapeHTML(d.displayName)}</h2><p class="muted">${escapeHTML(d.userId)}</p><span class="badge">Week ${d.currentWeek} · Day ${d.currentDay}</span></article><form class="card profile-form" id="profile-form"><label>Display name<input name="displayName" maxlength="40" value="${escapeHTML(d.displayName)}" required></label><label>Avatar<select name="avatar"><option value="finka.svg" ${d.avatar==='finka.svg'?'selected':''}>Finka avatar</option><option value="rahmat.svg" ${d.avatar==='rahmat.svg'?'selected':''}>Rahmat avatar</option></select></label><div class="form-grid"><label>Daily target (minutes)<input type="number" name="dailyTarget" min="5" max="180" value="${d.dailyTarget||20}"></label><label>Streak target (days)<input type="number" name="streakTarget" min="1" max="365" value="${d.streakTarget||30}"></label><label>Vocabulary target<input type="number" name="vocabularyTarget" min="30" max="5000" value="${d.vocabularyTarget||300}"></label></div><div class="profile-actions"><button class="btn btn-primary" type="submit">Save profile</button><button class="btn btn-danger" type="button" id="logout">Log out</button></div><p class="muted small">PIN values are sent only for secure backend verification and are never stored in frontend code.</p></form><form class="card profile-form pin-form" id="pin-form"><h2>Change PIN</h2><p class="muted">PIN changes are verified and stored only as a backend hash.</p><label>Current PIN<input type="password" inputmode="numeric" name="currentPin" minlength="4" maxlength="10" required></label><label>New PIN<input type="password" inputmode="numeric" name="newPin" minlength="4" maxlength="10" required></label><button class="btn btn-secondary" type="submit">Update PIN</button></form></section>`;root.querySelector('#logout').onclick=logout;root.querySelector('#profile-form').onsubmit=save;root.querySelector('#pin-form').onsubmit=savePin;refreshIcons()}catch(e){root.innerHTML=errorState(e.message);root.querySelector('[data-retry]')?.addEventListener('click',load)}}async function save(e){e.preventDefault();const b=e.submitter,before=b.textContent;b.disabled=true;b.textContent='Saving…';const fd=new FormData(e.currentTarget),payload=Object.fromEntries(fd);payload.dailyTarget=Number(payload.dailyTarget);payload.streakTarget=Number(payload.streakTarget);payload.vocabularyTarget=Number(payload.vocabularyTarget);try{await userApi.updateProfile(payload);updateSessionUser({displayName:payload.displayName,avatar:payload.avatar});showToast('Profile saved to Google Sheets.','success');load()}catch(err){showToast(err.message,'error');b.disabled=false;b.textContent=before}}
async function savePin(e){e.preventDefault();const b=e.submitter,fd=new FormData(e.currentTarget);b.disabled=true;b.textContent='Updating…';try{await userApi.updatePin(String(fd.get('currentPin')),String(fd.get('newPin')));e.currentTarget.reset();showToast('PIN updated securely.','success')}catch(err){showToast(err.message,'error')}finally{b.disabled=false;b.textContent='Update PIN'}}

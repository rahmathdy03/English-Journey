import{authApi}from'../api/auth-api.js';import{saveSession}from'../auth/session.js';import{redirectIfAuthenticated}from'../auth/auth-guard.js';import{showToast}from'../components/toast.js';import{refreshIcons}from'../utils/dom.js';
redirectIfAuthenticated();
const app=document.querySelector('#app');
app.innerHTML=`<main class="login-page"><section class="login-panel"><div class="login-title"><a class="landing-brand" href="../index.html"><img src="../assets/images/logo/logo-mark.svg" alt=""><span>English Journey</span></a><p class="eyebrow">Welcome back</p><h1>Who is learning today?</h1><p class="muted">Choose your profile, then enter your private PIN.</p></div><div class="profile-choice"><button class="card profile-card" type="button" data-user="finka" data-name="Finka"><img src="../assets/images/avatars/finka.svg" alt="Finka"><h2>Finka</h2><p class="muted">Continue Finka’s journey</p></button><button class="card profile-card" type="button" data-user="rahmat" data-name="Rahmat"><img src="../assets/images/avatars/rahmat.svg" alt="Rahmat"><h2>Rahmat</h2><p class="muted">Continue Rahmat’s journey</p></button></div><form class="card login-form" id="login-form" hidden><h2>Hi, <span id="selected-name">Learner</span></h2><label>PIN<input id="pin" name="pin" type="password" inputmode="numeric" pattern="[0-9]{4,10}" autocomplete="current-password" maxlength="10" placeholder="Enter your PIN" required></label><button class="btn btn-primary btn-block" id="login-submit" type="submit">Continue</button><p class="muted small">PIN validation happens securely in Google Apps Script.</p></form></section></main>`;
let selected='';const cards=[...document.querySelectorAll('[data-user]')],form=document.querySelector('#login-form'),pin=document.querySelector('#pin'),name=document.querySelector('#selected-name'),submit=document.querySelector('#login-submit');cards.forEach(card=>card.addEventListener('click',()=>{selected=card.dataset.user;cards.forEach(c=>c.classList.toggle('selected',c===card));name.textContent=card.dataset.name;form.hidden=false;pin.focus()}));form.addEventListener('submit',async e=>{e.preventDefault();if(!selected)return showToast('Choose a profile first.','error');const value=pin.value.trim();if(!/^\d{4,10}$/.test(value))return showToast('PIN must contain 4–10 digits.','error');submit.disabled=true;submit.textContent='Signing in…';try{const data=await authApi.login(selected,value);saveSession(data);location.replace('dashboard.html')}catch(err){showToast(err.message,'error');pin.select()}finally{submit.disabled=false;submit.textContent='Continue'}});refreshIcons();

import{initProtectedPage,pageHeader}from'../app.js';import{progressApi}from'../api/progress-api.js';import{loader,errorState}from'../components/page-loader.js';import{statCard}from'../components/progress-card.js';import{refreshIcons}from'../utils/dom.js';import{escapeHTML}from'../utils/sanitizer.js';
const root=initProtectedPage({active:'together',title:'Couple Goal'});if(root)load();async function load(){root.innerHTML=loader('Combining your progress…');try{const d=await progressApi.getCoupleGoal();root.innerHTML=`${pageHeader('Our Learning Goal','Progress built together, one lesson at a time.')}<section class="couple-hero card"><img src="../assets/images/illustrations/study-together.svg" alt=""><div><span class="badge success">Together</span><h2>${escapeHTML(d.message)}</h2><p>${escapeHTML(d.motivation)}</p></div></section><section class="stats-grid">${statCard('languages','Vocabulary together',d.totalVocabulary)}${statCard('calendar-heart','Study days together',d.totalStudyDays,'pink')}${statCard('flame','Combined streak',d.combinedStreak,'yellow')}${statCard('badge-check','Weeks completed',d.weeksCompleted,'lavender')}${statCard('target','Average score',`${Math.round(d.averageScore)}%`)}${statCard('award','Couple achievements',d.coupleAchievements,'pink')}</section><article class="card goal-progress"><div class="section-heading"><div><p class="eyebrow">Shared target</p><h2>${d.goal.current} / ${d.goal.target} vocabulary</h2></div><strong>${Math.round(d.goal.percent)}%</strong></div><div class="progress-track"><span style="width:${Math.min(100,d.goal.percent)}%"></span></div></article>`;refreshIcons()}catch(e){root.innerHTML=errorState(e.message);root.querySelector('[data-retry]')?.addEventListener('click',load)}}
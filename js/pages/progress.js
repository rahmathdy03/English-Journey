import{initProtectedPage,pageHeader}from'../app.js';import{progressApi}from'../api/progress-api.js';import{loader,errorState}from'../components/page-loader.js';import{statCard}from'../components/progress-card.js';import{refreshIcons}from'../utils/dom.js';
const root=initProtectedPage({active:'progress',title:'Progress'});let chart;if(root)load();async function load(){root.innerHTML=loader('Calculating your progress…');try{const d=await progressApi.getProgress(),s=d.summary;root.innerHTML=`${pageHeader('My Progress','See consistency, scores, and learning growth.')}<section class="stats-grid">${statCard('flame','Current streak',s.currentStreak)}${statCard('trophy','Best streak',s.bestStreak,'yellow')}${statCard('calendar-check','Study days',s.totalStudyDays,'pink')}${statCard('languages','Words learned',s.totalWords,'lavender')}${statCard('badge-check','Mastered words',s.masteredWords)}${statCard('list-checks','Quizzes completed',s.totalQuizCompleted,'yellow')}${statCard('target','Average score',`${Math.round(s.averageScore)}%`,'pink')}${statCard('spell-check-2','Grammar accuracy',`${Math.round(s.grammarAccuracy)}%`,'lavender')}</section><section class="progress-layout"><article class="card chart-card"><div class="section-heading"><div><p class="eyebrow">Scores</p><h2>Recent quiz results</h2></div></div><canvas id="score-chart" aria-label="Recent quiz score chart"></canvas></article><article class="card activity-card"><p class="eyebrow">Activity</p><h2>Latest learning days</h2>${(d.activity||[]).map(a=>`<div class="activity-row"><span>${a.date}</span><strong>Day ${a.day}</strong><span>${Math.round(a.finalScore||0)}%</span></div>`).join('')||'<p class="muted">No activity yet.</p>'}</article></section>`;drawChart(d.chart||[]);refreshIcons()}catch(e){root.innerHTML=errorState(e.message);root.querySelector('[data-retry]')?.addEventListener('click',load)}}function drawChart(points){const c=document.querySelector('#score-chart');if(!c||!window.Chart)return;c.height=130;chart?.destroy();chart=new Chart(c,{type:'line',data:{labels:points.map(x=>x.label),datasets:[{label:'Score',data:points.map(x=>x.score),tension:.35,fill:true}]},options:{responsive:true,scales:{y:{beginAtZero:true,max:100}},plugins:{legend:{display:false}}}})}
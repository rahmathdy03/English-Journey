import{initProtectedPage,pageHeader}from'../app.js';import{lessonApi}from'../api/lesson-api.js';import{loader,errorState}from'../components/page-loader.js';import{showToast}from'../components/toast.js';import{queryNumber}from'./page-utils.js';import{refreshIcons}from'../utils/dom.js';import{escapeHTML}from'../utils/sanitizer.js';
const root=initProtectedPage({active:'learn',title:'Grammar'}),week=queryNumber('week',1),day=queryNumber('day',1);if(root)load();async function load(){root.innerHTML=loader('Loading grammar…');try{const d=await lessonApi.getDailyLesson(week,day);if(d.locked)throw new Error('Complete the previous day first.');const g=d.lesson.grammar;root.innerHTML=`${pageHeader('Grammar Lesson',`Week ${week} · Day ${day}`,'<a class="btn btn-ghost" href="learning.html">Back to path</a>')}<article class="card grammar-card"><span class="badge">${escapeHTML(g.level||'Basic')}</span><h2>${escapeHTML(g.title)}</h2><p class="lead">${escapeHTML(g.function)}</p><div class="formula-box"><strong>Formula</strong><code>${escapeHTML(g.formula)}</code></div><section><h3>Simple explanation</h3><p>${escapeHTML(g.explanation)}</p></section><div class="example-grid">${example('Positive',g.positive,'check-circle')}${example('Negative',g.negative,'minus-circle')}${example('Question',g.question,'help-circle')}</div><div class="mistake-box"><h3><i data-lucide="triangle-alert"></i> Common mistake</h3><p>${escapeHTML(g.commonMistake)}</p></div><div class="mini-example"><strong>Mini example:</strong> ${escapeHTML(g.miniExample)}</div></article><div class="lesson-actions"><a class="btn btn-ghost" href="vocabulary-lesson.html?week=${week}&day=${day}">Review vocabulary</a><button class="btn btn-primary" id="complete">Finish grammar</button></div>`;document.querySelector('#complete').onclick=async()=>{const b=document.querySelector('#complete');b.disabled=true;b.textContent='Saving…';try{await lessonApi.completeGrammarLesson(week,day);showToast('Grammar lesson completed.','success');location.href=`practice.html?week=${week}&day=${day}`}catch(e){showToast(e.message,'error');b.disabled=false;b.textContent='Finish grammar'}};refreshIcons()}catch(e){root.innerHTML=errorState(e.message);root.querySelector('[data-retry]')?.addEventListener('click',load)}}function example(t,x,i){return`<div class="example-tile"><i data-lucide="${i}"></i><strong>${t}</strong><p>${escapeHTML(x)}</p></div>`}
import{refreshIcons}from'../utils/dom.js';export function showModal({title,content,actions=[]}){const r=document.getElementById('modal-root');r.innerHTML=`<div class="modal-backdrop"><div class="modal" role="dialog"><div class="modal-header"><h2></h2><button class="modal-close" aria-label="Close">×</button></div><div class="modal-body"></div><div class="cluster modal-actions"></div></div></div>`;r.querySelector('h2').textContent=title;r.querySelector('.modal-body').innerHTML=content;r.querySelector('.modal-close').onclick=()=>r.innerHTML='';actions.forEach(a=>{const b=document.createElement('button');b.className=`btn ${a.className||'btn-primary'}`;b.textContent=a.label;b.onclick=()=>{a.onClick?.();if(a.close!==false)r.innerHTML=''};r.querySelector('.modal-actions').append(b)});refreshIcons()}
export const loader=(m='Loading your progress…')=>`<div class="loader-wrap"><div><div class="spinner"></div><p class="muted">${m}</p></div></div>`;export const errorState=m=>`<div class="error-state"><h3>Progress could not be loaded.</h3><p class="muted">${m}</p><div class="spacer"></div><button class="btn btn-secondary" data-retry>Retry</button></div>`;export const emptyState=(t,m)=>`<div class="empty-state"><h3>${t}</h3><p class="muted">${m}</p></div>`;